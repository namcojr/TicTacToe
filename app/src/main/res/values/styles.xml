<resources>
    <!-- Material style for game board buttons -->
    <style name="TicTacToeBoardButton" parent="Widget.MaterialComponents.Button">
        <item name="android:layout_width">80dp</item>
        <item name="android:layout_height">80dp</item>
        <item name="android:textSize">32sp</item>
        <item name="android:textColor">?android:textColorPrimary</item>
        <item name="backgroundTint">?attr/colorPrimary</item>
        <item name="cornerRadius">12dp</item>
        <item name="elevation">2dp</item>
    </style>
    <!-- Material style for main action buttons -->
    <style name="TicTacToeActionButton" parent="Widget.MaterialComponents.Button">
        <item name="android:layout_width">200dp</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:minHeight">60dp</item>
        <item name="android:paddingTop">14dp</item>
        <item name="android:paddingBottom">14dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">?android:textColorPrimary</item>
        <item name="backgroundTint">?attr/colorPrimary</item>
        <item name="cornerRadius">12dp</item>
        <item name="elevation">4dp</item>
    </style>
    <!-- Alert dialogs: the app themes set textColorPrimary to the "on primary" colour
         (e.g. white), which is unreadable on the dialog surface. Reset text and button
         colours to neutral values that follow light/dark mode. -->
    <style name="ThemeOverlay.TicTacToe.AlertDialog" parent="ThemeOverlay.MaterialComponents.Dialog.Alert">
        <item name="android:textColorPrimary">@color/dialog_text</item>
        <item name="android:textColorSecondary">@color/dialog_text</item>
        <item name="buttonBarPositiveButtonStyle">@style/TicTacToeDialogButton</item>
        <item name="buttonBarNegativeButtonStyle">@style/TicTacToeDialogButton</item>
        <item name="buttonBarNeutralButtonStyle">@style/TicTacToeDialogButton</item>
    </style>
    <style name="TicTacToeDialogButton" parent="Widget.MaterialComponents.Button.TextButton.Dialog">
        <item name="android:textColor">@color/dialog_text</item>
    </style>
    <!-- Shared by the initials input and its ghost text so the letters line up exactly.
         Text is start-aligned; GameActivity pads it so "AAA" sits centred. -->
    <style name="InitialsText">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">start|center_vertical</item>
        <item name="android:paddingTop">14dp</item>
        <item name="android:paddingBottom">14dp</item>
        <item name="android:letterSpacing">0.4</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">34sp</item>
        <item name="android:maxLines">1</item>
    </style>
</resources>
